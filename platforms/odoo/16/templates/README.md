# Extracted template XML

These XML files are examples extracted from installer constants, not guaranteed live database exports. For a real system, run the read-only export scripts after installation and save the live export separately.
