# VAT extracted XML

`VAT_ARCH_*` and `LAYOUT_ARCH_*` are pure QWeb XML fragments extracted from the installer constants. Live database export output belongs under `support/backup-restore-export/.../examples/`, not this folder.
